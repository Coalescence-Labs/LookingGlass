---
slug: adaptive-music
date: 2026-06-17
agent: cursor
status: complete
---

## Scope

Humanize pass over `/adaptive-music` copy: PageHeader lede, short answer,
§ I–§ V intros and body, interactive captions (`AudioEngine`,
`ParameterDrivers`, `StateTree`), lib editorial strings (`STEMS`, `SCENES`,
`CASE_STUDIES`, `MIDDLEWARE`), `metadata.description`, and `concepts.ts`
subtitle.

## Files touched

- `src/app/adaptive-music/page.tsx` (lede tightened)
- (reviewed, no change needed) `src/components/adaptive/*`, `src/lib/adaptive-music.ts`,
  `src/lib/concepts.ts`

## AI tells

Ran the checklist scan (`rg -i` for delve/landscape/leverage/robust/"worth
noting"/"furthermore"/"cutting-edge"/etc.) across the page, components, and lib:
**no hits**. The draft was written to voice from the start, so this pass was
mostly verification plus one rhythm fix.

| Severity | Tell | Location | Fix |
|----------|------|----------|-----|
| Low | Comma-stacked lede ("…you hear, and when, while you play") | PageHeader lede | Trimmed the trailing clause; "which of them you hear, and when." |

No high- or medium-severity tells found.

## Before / after — lede

**Before**
> Play the same room twice and the music isn't quite the same. That's not a long
> recording on shuffle — it's a system. A composer writes the music in separable
> parts, and the game decides which parts you hear, and when, while you play.

**After**
> Play the same room twice and the music isn't quite the same. That's not one
> long recording on shuffle — it's a system. A composer writes the score in
> separable parts, and the game decides which of them you hear, and when.

## Voice checks

- Opens with a reframing (recording → system); single metaphor (a board you
  drive) sustained through § I.
- Concrete anchors: "the same eight bars", "the drums kick in for combat",
  "a DJ's talk-over", "the rustle of leaves".
- Honest hedging on the only soft numbers: ducking fade times given as "roughly
  50–200 ms / 500–1500 ms"; RDR2 hours/missions kept in sources as "roughly"
  and "around", out of the body.
- Varied sentence length; section intros ≤ ~45 words; lede ~55 words.
- Sources prose stays documentary.

## Deferred to review

- None. Facts unchanged from research; numbers and proper nouns identical to
  pre-pass.

## Verify

```text
bun run lint  → exit 0
bun run build → exit 0
```
