---
slug: mesh-networks
date: 2026-06-17
agent: cursor
pass: 1
---

## Scope

Polished every reader-facing string in `src/app/mesh-networks/page.tsx`,
`src/components/mesh/*`, and editorial fields in `src/lib/mesh.ts`. Facts,
numbers, and proper nouns frozen — this pass only touched voice and rhythm.

## Voice references consulted

- `src/app/context-window/page.tsx` (lede, § intros, sources prose)
- `src/app/one-million-tokens/page.tsx` (short-answer display poetry)
- `src/components/context-window/WhyItMatters.tsx` (card body register)

## AI-tells scan

Ran the checklist scan command against the article files:

```
rg -i 'delve|landscape|navigate|leverage|utiliz|robust|holistic|comprehensive|
  pivotal|crucial|vital|essential|it.s important|worth noting|in conclusion|
  let.s explore|when it comes to|ever-evolving|fast-paced|furthermore|moreover|
  additionally|seamless|game-chang|cutting-edge|a variety of|a range of|
  play a key role|tapestry|realm|in order to|due to the fact'
```

Result: **no hits.** Draft was written in-voice, so this pass is light polish
rather than de-robotising.

## High-severity tells fixed

None present.

## Edits made (rhythm / clarity)

| Location | Before | After | Why |
|----------|--------|-------|-----|
| PageHeader lede | "…finds its way across the web one hop at a time… what we'd build from the pieces…" | "…picks its way across the network one hop at a time… what we'd salvage…" | "web" was ambiguous with the WWW; "salvage" is more concrete and sets up § III |

## Voice checks (passing)

- **Reframe opener:** "Most networks have a middle… A mesh has none." ✓
- **One sustained metaphor:** the missing centre / "hop by hop" runs the piece. ✓
- **Concrete anchors:** "a mesh you can fit in a backpack", "a drive carries the
  bundles the radio can't", paperback-style specifics (37,600 nodes; 73,000 km). ✓
- **Honest limitation stated plainly:** the § III "what doesn't survive" column
  (streaming latency, CDN apps, card payments). ✓
- **Rhythm variation:** short punches ("A mesh has none.", "Different, not
  restored.") against longer mechanism sentences. ✓
- **Sources kept dry:** methodology bullets, no rhetoric. ✓
- **Curly punctuation:** `&rsquo;`, `&ldquo;`/`&rdquo;` throughout JSX. ✓

## Before / after samples

**Short answer (kept — already in-voice):**
> A mesh is a network with no centre. Every node *relays for its neighbours*, so
> a message hops from node to node across whatever links are up — and reroutes
> the moment one goes down.

**§ III opener (kept):**
> Now the second half — *what a mesh could be.* Everything from here on is a
> thought experiment, not a report.

## Deferred to review

- Nothing tone-related. Review should confirm the IEEE 802.15.4 and
  open-mesh.org source links resolve (curl), and re-verify all numbers against
  `research/mesh-networks/`.

## Verification

```
bun run lint  → exit 0
bun run build → exit 0 (/mesh-networks prerendered)
```
