---
slug: espresso-machine
date: 2026-06-17
agent: cursor
pass: 1
status: complete
---

## Scope

Voice pass over `/espresso-machine`. The draft was written in-voice during
implementation (concrete anchors, honest hedging, varied rhythm), so this pass
verified against the AI-tells checklist and the voice guide and made targeted
refinements rather than a wholesale rewrite. Facts, numbers, and citations
unchanged.

## Files touched

- `src/lib/espresso.ts` — one editorial string (boiler essence) softened to
  remove a faint brochure tone.

(`src/app/espresso-machine/page.tsx` and the three components were reviewed
string-by-string; no high- or medium-severity tells found that warranted edits.)

## AI-tells scan

Command:

```
rg -i 'delve|landscape|navigate|leverage|utiliz|robust|holistic|comprehensive|
pivotal|crucial|it.s important|worth noting|in conclusion|let.s explore|
when it comes to|furthermore|moreover|additionally|simply put|in order to|
a variety of|a range of|game-chang|cutting-edge|seamless' src/app/espresso-machine
src/components/espresso src/lib/espresso.ts
```

Result: **no true positives.** The only matches were the word "robusta" (the
coffee species) matching the substring "robust" — not the filler adjective.

## High-severity tells fixed

0 — none present in the draft.

## Edits made

| File | Before | After | Reason |
|------|--------|-------|--------|
| `espresso.ts` (BOILERS dual `essence`) | "Two boilers, no compromise" | "Two boilers, kept apart" | "no compromise" reads as marketing copy; the replacement is concrete and matches the mechanism (the boilers are physically independent) |

## Before/after — voice spot-checks (unchanged, cited for the record)

**Lede** (kept):
> Set aside the chrome and the ritual and an espresso machine is a small, exact
> instrument: it holds water a few degrees below boiling, leans on it with the
> weight of nine atmospheres, and pushes it through a thimble of ground coffee
> before the pressure has time to settle. Everything interesting happens in
> about twenty-five seconds.

Reframe + concrete anchors ("thimble", "the weight of nine atmospheres") + a
short closing punch after a long sentence. Matches the archive rhythm.

**§ IV intro / heading** (kept):
> Finer isn't *always* more. … grind finer and you expose more surface, which
> ought to raise the yield … But when [they] actually measured it, yield rose,
> peaked, and then *fell*.

Sets up the counter-intuitive finding without "Interestingly," or "It's worth
noting."

## Consistency

- Spelling: British `-our`/`-ise` used consistently (`flavour`, `stylised`).
- Terminology: one term per concept ("the puck", "the bed", "the shot",
  "crema") used consistently; no synonym cycling.
- The extraction curve and cutaway are each described once as illustrative /
  stylised, in both the component and the `#sources` notes — deliberate, for
  honesty, not synonym churn.

## Deferred to review

- None on voice. The § IV caveat paragraph (6-bar trials vs the 9-bar standard)
  is deliberately a little dense because the underlying fact is subtle; review
  should confirm it reads as accurate rather than hedging.

## Verification

```text
bun run lint  → exit 0
bun run build → exit 0 (/espresso-machine prerendered static)
```
