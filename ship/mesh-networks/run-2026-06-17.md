---
topic: "Mesh networks (and what we'd build if the stack fell)"
slug: mesh-networks
plan: plans/concepts/mesh-networks.md
linear-id: (manual — not tracked in this run)
linear-url:
date: 2026-06-17
agent: cursor
branch: plan/mesh-networks
status: in-progress
pr-url:
pr-number:
head-sha: 4fa94c5
review-verdict: approve-with-nits
---

## Summary

Shipping `/mesh-networks` as a Series III concept article: a two-part
editorial on how mesh networks actually route traffic today (mechanism)
and what a bottom-up rebuild of the stack could look like (speculation).
Centrepiece is an interactive topology where the reader deletes nodes and
watches a Dijkstra-routed path reroute or fail. PR opens against `develop`
for manual maintainer review.

## Environment note

The default `/workspace` worktree shares its `.git` with sibling agents
shipping other articles in parallel; branch refs there churned mid-run. To
avoid interfering with siblings and to get a stable tree, this run executes
in an isolated clone at `/home/ubuntu/mesh-ship`, working only on the
uniquely-owned branch `plan/mesh-networks` and pushing to its own origin
branch.

## Phase log

| Phase | Skill / action | Status | Commit | Pushed | Notes |
|-------|----------------|--------|--------|--------|-------|
| 0 | Resolve topic | ✅ | — | — | slug from plan path; Linear manual |
| 1 | Linear + git setup | ✅ | 9d9ea62 | ✅ | isolated clone; claim plan |
| 2 | article-research | ✅ | | ✅ | 6 chunks + numbers + visuals |
| 3 | article-design | ✅ | | ✅ | 5 sections + components.md |
| 4 | Implement | ✅ | | ✅ | lint/build/test all green |
| 5 | article-humanize | ✅ | | ✅ | in-voice draft; light polish, no AI tells |
| 6 | article-review | ✅ | | ✅ | approve-with-nits; no Criticals |
| 7 | PR opened | ⏳ | | | |

## Linear history

Linear handled manually by maintainer for this run (no MCP update performed).

## Artifacts

| Path | Status |
|------|--------|
| `research/mesh-networks/` | complete (draft) |
| `design/mesh-networks/` | complete (draft) |
| `humanize/mesh-networks/` | complete |
| `reviews/mesh-networks/` | complete — approve-with-nits |
| `src/app/mesh-networks/` | complete |

## Verification commands

```text
bun run lint  → exit 0 (Phase 4)
bun run build → exit 0 — /mesh-networks prerendered (Phase 4)
bun test      → exit 0 — 5 pass (Phase 4)
```

## Open items for maintainer

- Linear ticket update is manual for this run.

## Merged

*(Maintainer fills after merge.)*
