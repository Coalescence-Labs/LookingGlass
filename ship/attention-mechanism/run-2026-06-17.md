---
topic: How attention actually works
slug: attention-mechanism
plan: plans/concepts/attention-mechanism.md
linear-id: manual
linear-url: manual
date: 2026-06-17
agent: cursor
branch: plan/attention-mechanism
status: in-progress
pr-url: https://github.com/Coalescence-Labs/LookingGlass/compare/develop...plan/attention-mechanism
pr-number: 
head-sha: 149a8914c5bd236fc6bf3acbc56da23bcf61895b
review-verdict: approve-with-nits
linear: manual
---

## Summary

Shipping `/attention` as Concept 05 in Series I — On language models. An
interactive attention-matrix heatmap (hover a token, see what it attends to)
plus a head selector and a scaled dot-product equation card make the
transformer's core mechanism legible. All attention weights are precomputed
and shipped as static JSON — no runtime inference. Awaiting maintainer manual
review.

## Environment note

Work is performed in an isolated git worktree (`/home/ubuntu/att-wt`) on
branch `plan/attention-mechanism` because the shared `/workspace` checkout is
used concurrently by other ship-article agents (branches: espresso-machine,
mesh-networks, adaptive-music, testing-suite). The worktree gives this run an
independent HEAD/index so concurrent `git checkout` operations cannot clobber
it. node_modules is symlinked from `/workspace`.

## Phase log

| Phase | Skill / action | Status | Commit | Pushed | Notes |
|-------|----------------|--------|--------|--------|-------|
| 0 | Resolve topic | ✅ | — | — | slug: attention-mechanism, route /attention |
| 1 | Linear + git setup | ✅ | pending | pending | linear: manual (MCP not authenticated) |
| 2 | article-research | ✅ | pending | pending | 7 files, 13 claims, 10 sources |
| 3 | article-design | ✅ | pending | pending | 6 sections + components.md; route /attention |
| 4 | Implement | ✅ | pending | pending | page+3 components+lib+JSON+concepts; lint/build/test pass |
| 5 | article-humanize | ✅ | pending | pending | 0 high tells; 1 medium fix ("vote"→"single answer") |
| 6 | article-review | ✅ | pending | pending | approve-with-nits; 0 critical; links 200; CI green |
| 7 | PR opened | ⚠️ | n/a | n/a | branch pushed; `gh pr create` blocked (read-only token) + Github MCP down — compare URL below |
| 8 | Linear updated | manual | — | — | MCP not authenticated — paste comments above |
| 9 | Handoff | ✅ | — | — | returned to parent |

## Linear history

| When | Action | State |
|------|--------|-------|
| start | manual — MCP not authenticated | see fallback comment below |

## Linear fallback comment (for maintainer to paste at start)

```
🤖 ship-article pipeline started

- Plan: plans/concepts/attention-mechanism.md
- Branch: plan/attention-mechanism
- Route: /attention (Concept 05, Series I — On language models)
- Agent: Cursor cloud agent
- Linear: manual (MCP not authenticated in this environment)
- First commit pushed to origin before Phase 2.
```

## Linear fallback comment (for maintainer to paste at PR open)

```
🤖 ship-article PR ready for manual review

- PR: <PR URL>
- Branch: plan/attention-mechanism
- Review verdict: <verdict>
- Run log: ship/attention-mechanism/run-2026-06-17.md
- Do not auto-merge — maintainer reviews manually.
```

## Artifacts

| Path | Status |
|------|--------|
| `research/attention-mechanism/` | complete (draft) |
| `design/attention-mechanism/` | complete (draft) |
| `humanize/attention-mechanism/` | complete |
| `reviews/attention-mechanism/` | complete (approve-with-nits) |
| `src/app/attention/` | complete |

## Verification commands

```text
bun run lint  → exit 0 (Phase 4)
bun run build → exit 0 (Phase 4) — /attention prerendered static
bun test      → exit 0 (Phase 4) — 12 pass / 0 fail (7 new attention tests)
```

## PR creation blocker

`gh pr create` failed with `Resource not accessible by integration
(createPullRequest)` — the gh token in this environment is read-only. The
Github MCP server was also in an `error` state (no write tools available). The
branch `plan/attention-mechanism` is fully pushed and CI-green, so the PR can be
opened by the maintainer (or auto-opened by the remote environment) from:

- Compare / open PR: https://github.com/Coalescence-Labs/LookingGlass/compare/develop...plan/attention-mechanism
- Base: `develop` · Head: `plan/attention-mechanism` · Head SHA: `149a891`

PR title and body are drafted in Phase 9 handoff (below) / the PR template the
agent attempted.

## Open items for maintainer

- Open the PR from the compare URL above (agent token lacked write access).
- Nits from review (non-blocking): control touch-target sizing (~40px); verbose
  per-cell aria-labels on the matrix (acceptable for a data viz).
- Manual copy/visual adjustments on PR branch as desired.

## Merged

*(Maintainer fills after merge.)*
