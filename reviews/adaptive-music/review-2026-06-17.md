---
topic: How game music follows you (Adaptive music in games)
slug: adaptive-music
route: /adaptive-music
plan: plans/concepts/adaptive-music.md
research: research/adaptive-music/
design: design/adaptive-music/
date: 2026-06-17
agent: cursor
branch: plan/adaptive-music
commit: 4b47afc
verdict: approve-with-nits
status: resolved
---

## Verdict

**Approve with nits** — every plan acceptance bullet is met, all body claims
trace to research, lint/build/test are green, and the route builds; the only
open items are non-blocking (curl 403s from a bot-blocking host that resolve
live in a browser, sub-44px button heights matching the existing site, and the
fact that live keyboard/SR passes can't run without a dev server).

> Self-review gate (implementer + reviewer in one session). Maintainer still
> reviews the PR manually.

## Critical (must fix before merge)

*(None.)*

## Fact check

**Summary:** 14 page-worthy claims checked — 14 Match, 0 Drift, 0 Unsourced,
0 Unverifiable. Numbers in the body are deliberately few; the soft tertiary
figures (RDR2 hours/missions) are quarantined in `#sources` with hedges.

| Claim ID | Page location | Status | Evidence |
|----------|---------------|--------|----------|
| C-01 | Short answer ("reacts to game state, not buttons") | Match | Collins, *Game Sound* / *From Pac-Man to Pop Music* (interactive vs adaptive) — research chunk 01 |
| C-02 | Short answer + § I ("stems, mixed live") | Match | Audiokinetic vertical remixing; Wwise 201 L2 — chunk 02 |
| C-02a | `lib.captionFor` / pad "muted not stopped" | Match | White Rose Ch.3; Wwise 201 — chunk 02 |
| C-03 | § II ("cuts a piece into segments… jumps between them") | Match | Wwise 201 L1; White Rose Ch.3 — chunk 03 |
| C-04 | § II ("transition matrix… rule for every move") | Match | Wwise "Creating interactive music"; 201 L6 — chunk 03 |
| C-04a | § II ("waits for the current segment's exit cue") | Match | Wwise 201 L6 (default exit-cue rule) — chunk 03 |
| C-05 | § I + § II ("quantised to the beat or the bar") | Match | Wwise 201 L6; FMOD quantization — chunk 03 |
| C-06 | § II ("stingers… held back… land on the next beat") | Match | Wwise 201 L5 (stingers) — chunk 03 |
| C-07 | § III ("voice bus pulls the music bus down… DJ") | Match | Wwise Sidechaining tutorial; MCV — chunk 04 |
| C-07b | § III ("~50–200 ms… 500–1500 ms") | Match (hedged) | Amplitude SDK practitioner guidance; presented as ranges — chunk 04 |
| C-08 | § III ("HDR… gunshot… leaves drop out") | Match | Wwise "Understanding HDR" — chunk 04 |
| C-10/a/b | § IV Hades card + § I framing | Match | Korb interview (gameplay.co): drums on in combat; 4/3/3 pieces; semi-random stems — chunk 06 |
| C-11/a/b | § IV RDR2 card | Match | Rockstar Q&A (THR): 11 stems, no fixed key/tempo, Gunfight Conductor; orig RDR 5 stems — chunk 06 |
| C-12 | § IV Destiny/Halo card | Match | O'Donnell (Singer): compose linear then disassemble; custom engine — chunk 06 |
| C-13 | § V + engine ("context starts suspended… resume in click") | Match | MDN Web Audio best practices; Chrome autoplay — chunk 07 |
| C-14 | § I + § V + sources ("synthesised… no files") | Match | Implementation: `audio-engine.ts` generates all sound; no `public/audio/` — chunk 07 |

### Data-layer integrity

- All page numbers/labels read from `src/lib/adaptive-music.ts`; the scene
  shown, the tree node, and the caption all derive from `sceneFor` / `stemGains`
  (single source of truth) — verified by `src/lib/adaptive-music.test.ts` (9
  tests, green).
- `concepts.ts` title/subtitle/kicker match the plan + design draft.
- No hardcoded magic numbers in components contradicting lib (BPM and gains live
  in lib/engine).

### House rules

- Original prose throughout; no scraped transcripts (the "music" is synthesised,
  not sampled).
- No Grok content (not an AI-series piece; n/a).
- Primary sources cited for every claim category; approximate figures hedged.

## Links

**Summary:** 8 external URLs checked — 6 × 200, 2 × 403 (bot-block, confirmed
live via browser fetch). Internal anchors verified.

| URL | Status | Linked from | Notes |
|-----|--------|-------------|-------|
| developer.mozilla.org/.../Web_Audio_API/Best_practices | 200 | page.tsx sources | OK |
| webaudio.github.io/web-audio-api/ | 200 | page.tsx sources | OK |
| www.fmod.com/docs | 200 | page.tsx sources | OK |
| gameplay.co/hades-… | 200 | lib + sources | OK |
| www.hollywoodreporter.com/.../red-dead-redemption-2-… | 200 | lib + sources | OK |
| www.nicholassinger.com/blog/halo-… | 200 | lib + sources | OK |
| www.audiokinetic.com/en/courses/wwise201/ | 403→live | page.tsx sources | Bot-blocked to curl; resolves in browser/WebFetch |
| www.audiokinetic.com/en/library/edge/?id=understanding_hdr | 403→live | page.tsx sources | Bot-blocked to curl; WebFetch returned Wwise Help docs |

Internal: `/adaptive-music` builds as a static route; `#sources` anchor present;
no broken internal links.

## Build & CI

```text
bun run lint  → exit 0
bun run build → exit 0  (route /adaptive-music emitted; TypeScript clean)
bun test      → exit 0  (14 tests across 3 files; 9 new for adaptive-music)
```

`.github/workflows/` (per the concurrent `cursor/testing-suite` work) runs lint
+ test + build on develop/main — the same three commands verified here.

## Accessibility

### Critical / High

- None found.

### Medium / Low

- **Low** — Control heights: range thumbs sit in ~38px padded tracks; buttons
  are `py-2` (~32–36px), marginally under the 44px touch ideal but consistent
  with existing site controls (e.g. `ChatFallout`).
- **Low** — Two `aria-live="polite"` updates (caption + scene); acceptable, not
  chatty, but worth a listen on a real SR.

### Manual passes

| Pass | Result | Notes |
|------|--------|-------|
| Keyboard navigation | Not run — code review only | Sliders are native range inputs (arrow keys); Start/Mute/Stinger are real buttons; global `:focus-visible` ring. No dev server permitted. |
| Screen reader | Not run — code review only | `<section aria-label>`, slider `aria-label`/`aria-valuetext`, `role="img"`+`aria-label` on all SVG diagrams, `SplitWords` exposes `aria-label`, caption `aria-live`. |
| Reduced motion | Pass (code) | `useReducedMotion()` gates meter/tree/beat transitions; global CSS collapses transitions; audio path untouched. |

Other static checks: one `<h1>` (PageHeader); heading order h1→h2→h3 with no
skips; decorative SVG strokes `aria-hidden`; status conveyed by text ("on"/"off",
node label) not colour alone.

## Suggestions

- Maintainer may want to bump button padding to `py-2.5`/`py-3` for a comfier
  44px touch target if a broader pass is done.
- Consider a real-device SR + keyboard pass before public launch (needs a
  preview deploy).

## Questions

- Series III is introduced here and by `plan/espresso-machine` in parallel;
  `concepts.ts` will need a merge so there's one `everyday-machines` SERIES entry
  and both concept records (espresso = 04, this = 05). Flagged for the
  maintainer — not a defect on this branch.

## Acceptance mapping

| Plan acceptance bullet | Status | Evidence |
|------------------------|--------|----------|
| `bun run build` clean | Met | exit 0; route emitted |
| No copyrighted audio; original/CC0 with attribution | Met | All sound synthesised in `audio-engine.ts`; no files; stated in `#sources` |
| Audio opt-in — never starts without a user gesture | Met | `AdaptiveEngine.start()` + `ctx.resume()` only in the Start click |
| Mute persists across reveals | Met | `localStorage["lg-adaptive-muted"]`, read on start + on toggle |
| State-tree stays in sync with what's heard | Met | `sceneFor`/`stemGains` single source for tree, scene, caption, audio |
| Reduced motion disables animated crossfades, keeps audio | Met | `useReducedMotion()` gates visuals only |
| Page works muted — captions describe each state | Met | `captionFor` + `aria-live` caption; stem rack on/off text |
| Landing page groups concept under Series III | Met | `SERIES` + concept entry; home `conceptsBySeries()` renders it |

## Resolved

```
- 2026-06-17 · commit 4b47afc · self-review gate complete; no Critical items;
  verdict approve-with-nits. Audiokinetic 403s confirmed live via WebFetch.
```
