---
slug: espresso-machine
date: 2026-06-17
agent: cursor
branch: plan/espresso-machine
commit: (pre-review HEAD f5aac27; one fix applied during review)
verdict: approve-with-nits
critical: 0
---

# Review — How an espresso machine works (`/espresso-machine`)

Self-review gate (same session as implementation; maintainer still reviews the
PR manually). One Critical was found **and fixed** during this pass (dead INEI
link); the article is otherwise clean.

## Scope

- Files: `src/app/espresso-machine/page.tsx`, `src/components/espresso/{Cutaway,
  PullCurve,BoilerComparison}.tsx`, `src/lib/espresso.ts`,
  `src/lib/concepts.ts` (Series III + concept 04).
- Plan: `plans/concepts/espresso-machine.md` (Acceptance = contract).
- Research: `research/espresso-machine/` (claim ledger). Design:
  `design/espresso-machine/`.

## Phase 2 — Fact check

Representative claim sample (all hero/short-answer/axis figures included):

| Figure on page | Claim | Source | Result |
|----------------|-------|--------|--------|
| 9 bar ≈ 130 psi ≈ 9× atmosphere | C-03 | Clive / arithmetic | Match |
| 7–9 g · 92–95 °C · 20–30 s · 25–35 mL (SCA) | C-01 | Cameron & Hendon 2020 p.631 | Match |
| INEI 7 g · 88 °C · 9 bar · 25 s · 25 mL | C-02 | Istituto Espresso Italiano | Match |
| café 18 g in / 36 g out (15–22 g range) | C-10 | Cameron & Hendon 2020 | Match |
| ~30 % extraction ceiling | C-07 | Cameron & Hendon 2020 | Match |
| up to 25 % less coffee (downdosing) | C-09 | Cameron & Hendon 2020 | Match |
| EY peaks then falls with finer grind | C-08 | Cameron & Hendon 2020 Fig. 4 | Match |
| tamp force ~no effect on yield | C-13 | Cameron & Hendon 2020 | Match |
| 6-bar café trials vs 9-bar standard | discrepancy #2 | Cameron & Hendon 2020 | Match (framed honestly in §IV) |
| brew ~90–96 °C; steam ~125–135 °C; dual ±1 °C | C-05, C-06 | Coffee Scholars / Brew Precision | Match |
| pump ~60 strokes/s | C-04 | Clive / Coffeeionado | Match ("roughly sixty") |
| crema ≥10 % of cup; ≥2 min; 90 % droplets <10 µm | C-11, C-12 | Illy & Navarini 2011 | Match |
| crema = roast-CO₂ O/W foam; surfactant roles | C-11, C-11b | Illy & Navarini 2011 / FRI 2018 | Match |

- **Orphan claims on page:** none. Every number traces to a ledger ID.
- **Data-layer integrity:** prose figures and `src/lib/espresso.ts` constants
  agree; `concepts.ts` subtitle/kicker match the plan/design; no contradictory
  magic numbers in components.
- **House rules:** original prose; no Grok (N/A, not an AI piece); primary
  sources present for every quantitative claim; approximations hedged
  ("about", "roughly", "a few degrees"); curve + cutaway explicitly labelled
  illustrative/stylised in §II/§I and `#sources`.
- **Do-not-use honoured:** no 18–22 % "golden" EY asserted for espresso.

Verdict for Phase 2: **pass.**

## Phase 3 — Links

All external URLs curl-checked 2026-06-17 (`-A LookingGlass-LinkCheck/1.0`):

| Status | URL | Note |
|--------|-----|------|
| 200 | doi.org/10.1016/j.matt.2019.12.019 | Cameron & Hendon 2020 (primary) |
| 200 | iei.coffee/espresso-italiano-certificato/ | **fixed** — see below |
| 200 | pmc.ncbi.nlm.nih.gov/articles/PMC3140933/ | Illy & Navarini 2011 |
| 200 | coffeescholars.com/single-boiler-vs-heat-exchanger-vs-dual-boiler/ | boilers |
| 200 | brewprecision.com/articles/single-boiler-vs-dual-boiler-vs-heat-exchanger | boilers |
| 200 | clivecoffee.com/blogs/learn/the-pump-the-heart-of-your-espresso-machine | pump |
| 403 | sciencedirect.com/science/article/abs/pii/S0963996918307105 | bot-block; resolves in browser (verified via WebFetch in research) — **nit** |

**Critical found & fixed:** the original INEI citation
`https://inei.coffee/en/The-Certified-Italian-Espresso.html` returned **404**.
The institute now publishes at `iei.coffee`; updated the source to the live,
authoritative `https://iei.coffee/espresso-italiano-certificato/` (200) and
adjusted the cite/label/note accordingly. Re-ran lint/build/test after the edit
— all green.

Internal links: `/` (Index) via `PageHeader`; `#sources` anchor present. No
sibling cross-links required (this opens a new series).

## Phase 4 — Build & CI

Run in the review clone after the fix:

```text
bun run lint   → exit 0
bun run build  → exit 0 — /espresso-machine prerendered as static (○)
bun test       → exit 0 — 5 pass / 0 fail
```

No TypeScript errors. New route present in build output. (No
`.github/workflows/` consulted beyond the documented commands.)

## Phase 5 — Accessibility (code inspection)

| Check | Result |
|-------|--------|
| One `<h1>` | ✓ — only via `PageHeader` |
| Heading order | ✓ — h1 → h2 (§) → h3 (sub/explainers); no skips |
| Interactive labels | ✓ — range inputs have `aria-label` + `aria-valuetext`; SVG buttons `aria-label`; boiler cards `aria-pressed`; steppers labelled |
| Decorative visuals | ✓ — flow-dash + phase bands `aria-hidden`; SVGs `role="img"` with `<title>`/`aria-label` |
| Focus visible | ✓ — global `:focus-visible`; SVG `<g tabIndex=0>` focusable |
| Reduced motion | ✓ — all three components use `useReducedMotion`; dash off, pulses gated; PullCurve keeps curves + movable instant cursor (plan acceptance) |
| Colour-only meaning | ✓ — boiler ✓/✗ carry `sr-only` yes/no; curve values mirrored as text in legend |
| Touch targets | ✓ mostly — stepper/card/source targets generous; range thumb 18 px (standard slider) — minor |
| Link purpose | ✓ — source links use descriptive citation text, not "click here" |

- **Keyboard:** code-review only (no dev server per house rules). Range sliders
  operable by arrows; cutaway nodes navigable via Arrow keys + Enter/Space;
  steppers and cards are real buttons. Recommend a maintainer keyboard pass on
  the live preview.
- **Screen reader:** `metadata.title` matches the `<h1>`; live regions
  (`aria-live="polite"`) on the three explainers announce changes;
  `aria-valuetext` reads "12 seconds · 9 bar · 2.4 millilitres per second · 93
  degrees".

No critical or high a11y findings.

## Phase 6 — Acceptance & design

| Plan acceptance | Status |
|-----------------|--------|
| `bun run build` clean | Met |
| Cutaway hand-drawn SVG, not from a manufacturer | Met (hand-authored; stated in `#sources`) |
| Pull-curve: dragging time cursor updates all three tracks | Met (scrub updates pressure/flow/temp read-outs + cursor dots) |
| Every pressure/temperature/time figure cited | Met (claim IDs in lib; sources on page) |
| Mobile-responsive; reduced-motion keeps data visible | Met |
| Landing page groups concept under Series III | Met (`SERIES` `everyday-machines`, roman III; concept `seriesId`) |

Design adherence: all six sections (short answer + §I–§V) and the three speced
interactives (`Cutaway`, `PullCurve`, `BoilerComparison`) implemented per
`design/espresso-machine/`.

## Verdict — Approve with nits

Critical: **0** (the INEI 404 was fixed during review). Nits below are
non-blocking.

### Nits / suggestions (non-blocking)

1. **ScienceDirect 403** to automated checkers (bot protection). The page is the
   real FRI 2018 crema study and resolves in browsers; it is a *secondary*
   corroboration of surfactant roles already attributed to Illy & Navarini 2011.
   Optional: swap for a DOI link if one is confirmed.
2. **Range thumb 18 px** is slightly under the 44 px touch ideal — standard for
   sliders on the site (matches `TokenScale`); acceptable.
3. **Pre-existing, out of scope:** the home page still reads "Series II arrives
   when it's ready" — unrelated to this article; flag for the maintainer.
4. Recommend a **live keyboard/SR pass** on the preview before merge (reviewer
   could only inspect code, per the no-dev-server rule).

## Commands to re-run after any fixes

```bash
bun run lint && bun run build && bun test
```
