---
slug: mesh-networks
plan: plans/concepts/mesh-networks.md
research: research/mesh-networks/
design: design/mesh-networks/
date: 2026-06-17
reviewer: cursor (automated gate, same session)
branch: plan/mesh-networks
head-sha: 4fa94c5
verdict: approve-with-nits
---

## Summary

`/mesh-networks` ships the Series III concept as specified: a two-half editorial
(mechanism + speculation), an interactive Dijkstra-routed topology, a protocol
comparison, and a first-party deployment map. All acceptance criteria met. CI
gate green. One bot-blocked source link (guifi.net, live in browser) and two
low-severity nits; **no Critical items**.

## Scope reviewed

| Path | Notes |
|------|-------|
| `src/app/mesh-networks/page.tsx` | prose, metadata, `#sources` |
| `src/components/mesh/TopologyDemo.tsx` | centrepiece interactive |
| `src/components/mesh/ProtocolCompare.tsx` | static table + glyphs |
| `src/components/mesh/DeploymentMap.tsx` | SVG map + pins + text list |
| `src/lib/mesh.ts` | topology, Dijkstra, deployments, matrices |
| `src/lib/concepts.ts` | Series III + concept entry |

`git diff origin/develop...HEAD` = the six files above plus
research/design/humanize/ship artifacts.

## Phase 2 — Fact check

Every page number traced to a research claim ID. Representative ledger:

| On page | Value | Claim | Source | Result |
|---------|-------|-------|--------|--------|
| LoRa rate | 0.3–50 kbps | C-01 | LoRa Alliance 1.0.3 | Match |
| LoRa range | ≈ 15 km LOS | C-03 | Semtech / Lee&Choi | Match (within band) |
| 802.15.4 rate | 250 kbit/s | C-04 | IEEE 802.15.4-2006 | Match |
| 802.15.4 range | ≈ 10–100 m | C-05 | IEEE/Thread refs | Match |
| Guifi nodes | ≈ 37,600 working | C-30 | guifi.net stats | Match (mid-2026) |
| Guifi links | ≈ 73,000 km | C-30 | guifi.net stats | Match |
| Guifi founded | 2004 | C-30c | Wikipedia | Match |
| NYC Mesh hubs | ≈ 70 | C-31a | NYC Mesh 2024 review | Match |
| NYC Mesh installs | 450+ (2024) | C-31b | NYC Mesh 2024 review | Match |
| NYC Mesh founded | 2014 | C-31d | NYC Mesh | Match |
| Meshtastic | LoRa managed flooding | C-32/C-15 | Meshtastic docs | Match |
| Reticulum | 128-bit pubkey hash | C-22 | Reticulum manual | Match |
| Web of trust | Zimmermann 1992 | C-21 | Wikipedia | Match |
| CA roots | "over a hundred" | C-20 | Wikipedia | Match |
| Pantheon | S2 homemade net | C-40 | Wikipedia Pantheon | Match |
| Walkaway | Doctorow 2017 | C-41 | Wikipedia | Match |

- **No orphan claims** on the page.
- **Dijkstra caveat present** in `#sources` (C-14/C-15): demo is a teaching
  model; real meshes route distributed; Meshtastic floods. Good — prevents
  mis-teaching.
- **House rules:** original prose only; no scraped third-party text; *Pantheon*
  and the cyberpunk revival credited as inspiration with an explicit no-lift note
  in `#sources`; no Grok (n/a). Pass.

### Data-layer integrity

`PHY_LAYERS`, `DEPLOYMENTS`, `TOPOLOGY_COMPARISON` strings all match research.
Topology is deterministic (seeded mulberry32) so SSR/CSR agree — verified by a
clean production build (no hydration warnings). `route()` confirmed to reroute
on node deletion and report unreachable when partitioned.

## Phase 3 — Link verification

HTTP checks (curl, `LookingGlass-LinkCheck/1.0`, 2026-06-17):

| Status | URL |
|--------|-----|
| 200 | datatracker.ietf.org/doc/html/rfc3561, rfc3626, rfc4838, rfc9171 |
| 200 | freifunk.net, meshtastic.org, reticulum.network, nycmesh.net |
| 200 | standards.ieee.org/ieee/802.15.4/7029/ |
| 200 | open-mesh.org/projects/batman-adv/wiki |
| 200 | semtech.com/…/lora-and-lorawan.pdf |
| 403 | guifi.net — **bot-blocked WAF** |

**guifi.net (403):** the canonical operator site blocks automated clients
(curl + browser UA both 403). It is live in a real browser, and its node/link
statistics were successfully retrieved during research. This is a bot-block, not
a dead link. **Manual verify required** (nit, non-blocking). Kept as the correct
primary citation.

Internal: `/mesh-networks` route builds; `#sources` anchor present; landing-page
cross-link via `concepts.ts` works.

## Phase 4 — Build & CI

```
bun run lint  → exit 0
bun run build → exit 0  (/mesh-networks prerendered as static)
bun test      → exit 0  (5 pass, 0 fail)
```

No TypeScript errors. New route appears in build output. No `.github/workflows`
gap identified beyond these commands.

## Phase 5 — Accessibility

| Check | Result |
|-------|--------|
| One `<h1>` | Pass — only PageHeader |
| Heading order | Pass — h1 → h2 (§) → h3 (sub), no skips |
| Interactive labels | Pass — topology nodes `role=button`+`aria-label`+`aria-pressed`; Reset is a real button; map pins are labelled links; compare is a real `<table>` with `th scope` |
| Decorative SVG | Pass — landmasses + glyphs `aria-hidden`; topology/map `role="img"` with descriptive labels |
| Live region | Pass — topology readout `aria-live="polite"` |
| Colour-only meaning | Pass — reachability uses ✕/● + text; survives/doesn't split by text |
| Focus visible | Pass — global `:focus-visible` + custom node focus stroke |
| Reduced motion | Pass — `useReducedMotion()` gates topology transitions; CSS collapses under `prefers-reduced-motion` |
| Keyboard | **Code-review only** (no dev server) — nodes operable via Enter/Space, pins via Tab; recommend a manual pass |
| Touch targets | Nit — node hit area ≈32px on the smallest phones (nodes are densely packed; enlarging would overlap) |

## Phase 6 — Design & acceptance

| Acceptance (plan) | Status |
|-------------------|--------|
| lint + build clean | Met |
| Deleting a bridge node changes path; enough deletions → unreachable | Met (verified) |
| Every §§ I–II technical claim cites a spec/deployment doc | Met |
| § III unambiguously labelled speculation | Met — visible "Speculative — not reportage" tag + hedged verbs throughout |
| *Pantheon* credited, no dialogue/frames/designs | Met |
| Cyberpunk refs in our own prose, nothing scraped | Met |
| Reduced motion collapses routing animation; static topology readable | Met |
| Landing page groups concept under Series III | Met |

Design parity: short answer + § I–IV + sources all implemented;
TopologyDemo, ProtocolCompare, DeploymentMap all present.

## Findings

### Critical
None.

### Nits (non-blocking)
1. **guifi.net 403** to automated clients (bot-block). Live in browser;
   manual-verify. Kept as canonical source.
2. **Touch targets** on the topology graph are ~32px on the narrowest phones.
3. **Freifunk founding year (2002)** is medium-confidence (lib data only, not in
   prose); acceptable.

## Verdict

**approve-with-nits.** No Critical items; CI green; facts and links verified
(one documented bot-block). Cleared for PR.
